#import "../../setup.typ": *
#show: setup.with()

= Study Session

Suggested path:

1. #link("./index.typ")[Read the overview]
2. #link("./observer.typ")[Review Observer]
3. #link("./iterator.typ")[Review Iterator]
4. #link("./strategy.typ")[Review Strategy]

#flashcard(
  [What does this example folder demonstrate?],
  [Preview navigation, graph links, and SRS flashcard extraction from linked Typst notes.],
)
